---
breaking_changes:
  - "Collection name changed from ``drts.ansible_collection`` to ``drts.linux``. Users must update their ``requirements.yml`` files and playbook references to use the new collection name ``drts.linux`` instead of ``drts.ansible_collection``."
