---
# Converge playbook for fedora_minimal role testing
# Applies the fedora_minimal role to the test container

- name: Converge fedora_minimal role
  hosts: all
  become: true
  gather_facts: true

  roles:
    - role: fedora_minimal
